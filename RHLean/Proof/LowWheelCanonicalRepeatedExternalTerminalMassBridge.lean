import RHLean.Proof.LowWheelCanonicalRepeatedTerminalHighPrimeBridge
import RHLean.Proof.LowWheelExternalTerminalEightRootBound

/-!
# Retired external-terminal mass experiment

The former theorem body in this module was an uncompiled experimental bridge
that no longer typechecked against the current repeated-parent API.  Its durable
inputs are the terminal high-prime bridge and the external eight-root bound,
which remain compiled above.  The stale duplicate bridge is intentionally not
reintroduced.
-/
