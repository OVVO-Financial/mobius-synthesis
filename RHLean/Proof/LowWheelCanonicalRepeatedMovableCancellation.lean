import RHLean.Proof.LowWheelCanonicalRepeatedMovableOthello

/-!
# Retired movable-cancellation experiment

The former declaration set depended on an earlier movable-toggle API and was
never reachable from the root build.  The maintained Othello formulation is
`LowWheelCanonicalRepeatedMovableOthello`, imported above.  Keeping this module
as a compatibility tombstone prevents stale proofs from being mistaken for an
open seam while preserving old module imports.
-/
